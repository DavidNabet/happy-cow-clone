import React from "react";
import { StyleSheet, View } from "react-native";

const OpenClosed = ({ description }) => {
  return <View></View>;
};

export default OpenClosed;

const styles = StyleSheet.create({});
